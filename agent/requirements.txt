pyserial>=3.5
psutil>=5.9

# opcionais, so' instalam no Windows (pip ja pula essas linhas no Linux/Mac):
# temperatura/fan/GPU via LibreHardwareMonitor, quando disponivel -- ver
# README, secao "Windows". Sem elas o agente roda igual, so' sem esses campos.
wmi>=1.5.1; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
